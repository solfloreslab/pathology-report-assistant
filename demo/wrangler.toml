name = "pathology-agent-api"
main = "worker.js"
compatibility_date = "2024-01-01"

# Secrets (set via: npx wrangler secret put OPENROUTER_API_KEY)
# Secrets (set via: npx wrangler secret put ACCESS_CODE)
# NEVER put actual keys here — use `wrangler secret put` only.

[vars]
ENVIRONMENT = "development"

# Dev-only: in production, use wrangler secret put
[env.dev.vars]
# Keys are loaded from .dev.vars file automatically
